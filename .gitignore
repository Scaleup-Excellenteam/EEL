__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# Corpus and build artifacts — never commit these
Archive/
Archive.zip
*.idx
*.index

# ZDT snapshots: versioned directories + the `current` pointer, all generated
# by build_snapshot.py — see src/snapshot.py.
snapshots/
