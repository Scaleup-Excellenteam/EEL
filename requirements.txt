# Runtime
streamlit>=1.37
SpeechRecognition>=3.10

# The core algorithm is standard library only.
# External libraries are permitted (SPEC.md 7.6) — add here if one earns its way in.

# Development
pytest>=8.0
